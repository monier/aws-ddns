# OS junk
.DS_Store

# Secrets
.env
aws-ddns.ini

# Transient build-secret files some container engines (e.g. Podman) leave behind when a
# `--secret` build is used — they can contain a token and must never be committed.
*build-secret-*

# AI plans and outputs (generated by ai-assist skills)
plans/

# App log files
logs/

# Build output
bin/
dist/

# Local app data folder (state file, local aws-ddns.ini)
data/
